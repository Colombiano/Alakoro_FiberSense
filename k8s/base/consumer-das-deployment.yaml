apiVersion: apps/v1
kind: Deployment
metadata:
  name: alakoro-consumer-das
  namespace: alakoro
  labels:
    app: alakoro-consumer-das
spec:
  replicas: 1
  selector:
    matchLabels:
      app: alakoro-consumer-das
  template:
    metadata:
      labels:
        app: alakoro-consumer-das
    spec:
      containers:
        - name: consumer
          image: alakoro/backend:latest
          command: ["python", "-m", "app.consumers", "--type", "das"]
          envFrom:
            - configMapRef:
                name: alakoro-config
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
